var main => {
  // pop、push、shift、unshift、slice、substring、substr
  // sort、reverse、splice、concat、join、indexOf、lastIndexOf、forEach、map、filter、some、every、reduce、reduceRight
  // copyWithin、includes、fill、find、findIndex
  // 
}

var __main = function() {
  // 数组
  // 栈
  // 队列
  // 链表 （链表存储有序的元素集合，但不同于数组，链表中的元素在内存中并不连续放置，每个元素
  //        由一个存储元素本身的节点和一个指向下一个元素的引用【也称指针或链表】组成）
  // 集合 (没有重复元素，也没有顺序概念的数组。)
  // 字典和散列表 （key, value）
  // 树 (二叉树和二叉搜索树，中序遍历，先序遍历，后序遍历，自平衡二叉搜索树, 红黑树, 堆积树)
  // 图（非线性数据）
  // 排序和搜索算法
  // 算法补充
}
